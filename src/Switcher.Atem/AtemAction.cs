namespace Switcher.Atem;

/// <summary>ATEM operations reachable from a mapped controller button (docs/specs/pc-switcher-app.md §2.6).</summary>
public enum AtemAction
{
    ProgramInput,
    PreviewInput,
    Cut,
    Auto,
}
